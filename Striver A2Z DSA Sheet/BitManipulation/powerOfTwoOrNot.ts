const a = 8;
const b = 7;

console.log(a.toString(2));
console.log(b.toString(2));

// here if number is power of 2 so in that number all are zero except 1 and minus one of that number
// all will be one so & should be zero of that. 


// Binary to decimal
console.log(parseInt("1000",2));